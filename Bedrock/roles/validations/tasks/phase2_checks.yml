# ============================================================
# FINAL STATUS OUTPUT
# ============================================================

- name: Get final nodes status (wide view)
  command: kubectl get nodes -o wide
  environment:
    KUBECONFIG: /etc/kubernetes/admin.conf
  register: final_nodes
  changed_when: false


- name: Display final nodes status
  debug:
    msg: "{{ final_nodes.stdout }}"